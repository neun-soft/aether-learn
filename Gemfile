source "https://rubygems.org"

# Pin fastlane so builds are reproducible across machines and CI.
# Homebrew's fastlane also works for local runs; bundler is the CI-friendly path.
gem "fastlane", "~> 2.228"
gem "xcodeproj"
