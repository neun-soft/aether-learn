import Foundation

// Translation table intentionally empty — app is English-only for now.
// Re-introduce per-language dictionaries here when other languages are needed.
enum Translations {
    static let table: [String: [Lang: String]] = [:]
}
