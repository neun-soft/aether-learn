app_identifier("app.neun.aether.learn")
team_id("7LAGLNXS6L")

# App Store Connect auth is handled by the App Store Connect API key in the
# Fastfile (app_store_connect_api_key), so no apple_id / itc_team_id is needed
# here. See fastlane/.env.example for the required environment variables.
